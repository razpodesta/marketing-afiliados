// tests/mocks/server-only.ts
/**
 * @file server-only.ts
 * @description Módulo de simulación para el paquete 'server-only'.
 *              Este archivo proporciona una exportación vacía para satisfacer
 *              al bundler (Vite) cuando ejecuta código de servidor en un
 *              entorno simulado (JSDOM), previniendo errores de importación.
 * @author L.I.A Legacy
 * @version 1.0.0
 */
export {};
/**
 * @section MEJORA CONTINUA
 *
 * @subsection Melhorias Futuras
 * 1. **Mocks de Otros Paquetes de Servidor**: ((Vigente)) Si se utilizan otros paquetes exclusivos de servidor (ej. 'server-http'), se pueden añadir mocks similares siguiendo este patrón.
 */
// tests/mocks/server-only.ts
