// tests/mocks/api/handlers.ts
/**
 * @file handlers.ts
 * @description Manifiesto de manejadores de peticiones para Mock Service Worker (MSW).
 *              Aquí se definirán todas las rutas de API simuladas.
 * @author L.I.A Legacy
 * @version 1.0.0
 */
import { http, HttpResponse } from "msw";

export const handlers = [
  // http.get('/api/user', () => {
  //   return HttpResponse.json({ name: 'John Doe' })
  // }),
];
